description: >
  Linux Container for cross compiling (clang) and running (wine) x86_64-pc-msvc for release jobs
docker:
  - image: ghcr.io/pascalkuthe/ferris_ci_win:latest
resource_class: << parameters.resource_class >>

parameters:
  resource_class:
    default: large
    type: string
