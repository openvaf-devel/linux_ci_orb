description: >
  Default Linux Container for release jobs (includes manylinux)
docker:
  - image: ghcr.io/pascalkuthe/ferris_ci:latest
resource_class: << parameters.resource_class >>

parameters:
  resource_class:
    default: large
    type: string
